Here is a suggestion for sub-directory names:

  o main - the main source repository
  o bin - sources to generate scripts from
  o etc - sources to generate configuration files from
  o sql - SQL schema definitions and such
  o doc - Document sources, for those that require generation
  o docroot - .jsp & .html files
  o resource - Resource files that are used by classes inside of 'main'

