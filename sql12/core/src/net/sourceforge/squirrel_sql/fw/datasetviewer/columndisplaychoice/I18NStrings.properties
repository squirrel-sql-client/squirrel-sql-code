ResultDataSetAndCellDetailDisplayHandler.display.choice.for.table.only=Display choice is available for table display only not for text display.
ResultDataSetAndCellDetailDisplayHandler.display.choice.when.query.result.selected.only=Display choice is available only when the query result tab is selected.

CellDetailDisplayAvailableInfo.cell.details.display.not.available=Cell detail display not available.

ShowCellDetailCtrl.toggle.show.cellDetail.tooltip=Toggle showing cell detail on the right of the result table

ResultImageDisplayPanel.image.display.error=Failed to read image from column {0} ({1}): {2}
ResultImageDisplayPanel.image.display.error.blob.not.read=Failed to read image from column {0} ({1}): {2}\n\n\
  Note that BLOBs are not read completely, see menu File --> Preferences --> "tab Data Type Controls" section BLOB
ResultImageDisplayPanel.image.display.error.clob.not.read=Failed to read image from column {0} ({1}): {2}\n\n\
  Note that CLOBs are not read completely, see menu File --> Preferences --> "tab Data Type Controls" section CLOB


ResultImageDisplayPanel.could.create.image=Could not create an image from the selected cell in column {0} ({1}).
ResultImageDisplayPanel.could.create.image.blob.not.read=Could not create an image from the selected cell in column {0} ({1}).\n\n\
  Note that BLOBs are not read completely, see menu File --> Preferences --> "tab Data Type Controls" section BLOB
ResultImageDisplayPanel.could.create.image.clob.not.read=Could not create an image from the selected cell in column {0} ({1}).\n\n\
  Note that CLOBs are not read completely, see menu File --> Preferences --> "tab Data Type Controls" section CLOB

ResultImageDisplayPanel.drop.image.file.here=Drop image file here
ResultImageDisplayPanel.drop.image.file.here.tooltip=Drop image file here to save the image to the selected cell in the database

ResultImageDisplayPanel.no.file.dropped=Drop did contain no file.

ResultImageDisplayPanel.failed.to.read.file=Failed to read file {0} into database: {1}

ResultImageDisplayPanel.delete.image=Delete image from database?
ResultImageDisplayPanel.delete.image.from.database.tooltip=Delete this image from the database

DisplayPanel.select.display=Select display

DisplayMode.default=Default
DisplayMode.image=Image

ResultImageDisplayPanel.null.blob.not.read={0}\nNote that BLOBs are not read completely, see menu File --> Preferences --> "tab Data Type Controls" section BLOB
ResultImageDisplayPanel.null.clob.not.read={0}\nNote that CLOBs are not read completely, see menu File --> Preferences --> "tab Data Type Controls" section CLOB

DisplayPanel.info.button=<html>Allows to display the column content as image.<br>\
  If a SQL result table is editable images can be dragged and dropped into the database by the dropping the image to the label at the bottom.<br>\
  If an image is stored in a CHAR type column base 64 encoding is assumed.</html>

CellDisplayPanel.pinned.tooltip=When pinned (toggle switched on) all cell details will be shown in this dialog.